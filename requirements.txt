# ── Веб-фреймворк ─────────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0     # ASGI-сервер (включает websockets, httptools)
python-multipart>=0.0.6       # Парсинг form-data в FastAPI

# ── Доступ к файлам ────────────────────────────────────────────────────────────
smbprotocol>=1.13.0            # Чтение аудиофайлов с SMB/CIFS-шар (Windows Fileserver)
aiofiles>=23.2.1               # Асинхронное чтение локальных файлов

# ── Конфигурация ───────────────────────────────────────────────────────────────
pyyaml>=6.0.1                  # Парсинг YAML-конфигов

# ── Аудио ─────────────────────────────────────────────────────────────────────
mutagen>=1.47.0                # Определение длительности аудиофайлов (MP3/WAV/AAC)

# ── Утилиты ───────────────────────────────────────────────────────────────────
python-dateutil>=2.8.2         # Парсинг дат и работа с временными зонами
sortedcontainers>=2.4.0        # SortedList для быстрого поиска в индексе файлов

# ── Аутентификация (опционально) ───────────────────────────────────────────────
# Требуется только при LDAP: true в config/ldap.yaml (авторизация через Active Directory)
ldap3>=2.9.1
# MD4 fallback для NTLM на Python 3.x + OpenSSL 3.x (OpenSSL 3 убрал MD4 из hashlib).
pycryptodome>=3.19.0

# ── Kerberos / GSSAPI (опционально, Linux) ────────────────────────────────────
# Нужен только при auth_protocol: kerberos в конфиге SMB-источника (кросс-доменный доступ).
# При обычном NTLM-подключении эти пакеты НЕ нужны.
# Установка: pip install -r requirements-kerberos.txt
# Системные зависимости перед установкой: sudo apt install gcc libkrb5-dev
