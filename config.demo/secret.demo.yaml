authorization:
  - id: 1
    username: "user1"
    password: "mypass"
    domain: "CONTOSO"
  - id: 2
    username: "logger2"
    password: "mypass2"
    domain: "NR.MEDIA"

  # Сервисный аккаунт AD для авторизации в Авокадо (read-only, просмотр каталога)
  # Используется при LDAP: true в ldap.yaml (параметр bind_secret)
  - id: 10
    username: "ldap"
    password: "ldap-password"
    domain: "CORP"
